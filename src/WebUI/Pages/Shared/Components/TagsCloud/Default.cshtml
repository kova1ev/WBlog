@model IEnumerable<PopularTagModel>

<div class="tags">
    @foreach (var tag in Model ?? Enumerable.Empty<PopularTagModel>())
    {
        <a asp-page="Index" asp-route-tag="@tag.Name" class="badge bg-success">@tag.Name</a>
    }
</div>